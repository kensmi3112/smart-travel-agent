# Core dependencies
streamlit
weasyprint
markdown

# API clients
openai          # Used by TravelRetirementAgent (compatible with Grok)
requests
python-dotenv   # For loading secrets (optional but useful)
